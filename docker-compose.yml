version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    volumes:
      - .:/app
    working_dir: /app
    tty: true
    #command: tail -F /dev/null
